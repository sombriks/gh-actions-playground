name: 13 - Create a comment on a pull request
on:
  pull_request:
    branches:
      - main
env:
  GH_TOKEN: ${{ github.token }}
jobs:
  comment-if-its-a-pr:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'running from ${{github.ref_name}}'
      - uses: actions/checkout@v4
      - run: gh pr list
